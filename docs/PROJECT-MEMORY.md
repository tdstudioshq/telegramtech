# PROJECT-MEMORY — Creator Platform

> Cross-session handoff. Read first every session; update before ending any session.

## Current state
- **Session 3 complete (2026-07-04): M1 implemented.** Gate was approved by Tyler; architecture docs are frozen source of truth.
- Delivered: pnpm/strict-tsconfig/ESLint(boundary zones)/Prettier/Vitest scaffold · CI workflow (typecheck→lint→unit) · `config/env.ts` Zod parse-or-crash + tests · Pino logger with redaction · `shared/` (Result, AppError, domain enums/ids) · `core/events` (typed events + after-commit dispatcher + EventBuffer) + tests. 25 unit tests green; boundary rules verified to fail on deliberate violations; parse-or-crash and redaction verified at runtime.
- Toolchain notes: ESLint pinned to v9 (eslint-plugin-import lacks v10 peer support); local Node is 26, `.nvmrc`/CI pin 22; deps: zod 4, pino 10, vitest 4, typescript 6.
- Conventions set in M1: ESM (`"type": "module"`, NodeNext, `.js` import specifiers) · `EventBuffer.drain()` after commit → `dispatcher.dispatchAll()` (rollback = never drain) · dispatcher takes a minimal `DispatcherLogger` via constructor (core stays logging-free) · Prettier never touches docs/README (frozen).
- **Session 4 (2026-07-04): M2 first half done.** DATABASE.md rev 2.2 approved. Added drizzle-orm/postgres/drizzle-kit · schema in `src/adapters/persistence/db/schema/` (12 physical tables — §11 holds two settings tables — 11 Postgres enums mirrored 1:1 from shared/domain with a drift-guard unit test) · migration `0000_right_katie_power.sql` generated, re-generate confirms no drift. 51 unit tests green. **STOPPED at the documented review point — migration NOT applied.** Interpretation flagged for Tyler: drop_assets storage_bucket/storage_path made nullable because the §4 CHECK ("text ⇒ text_content; media ⇒ storage path") contradicts their not-null annotation; CHECK enforces shape per content_type. bot_settings uses native `UNIQUE NULLS NOT DISTINCT` (PG15+) for the documented "coalesced unique index" intent. BRIN on audit_logs.created_at deliberately omitted (doc says "at volume").
- **Session 4 second half (2026-07-04): M2 code complete.** Migration review approved (drop_assets nullable-storage ruling confirmed). Built: repository interfaces in `core/repositories` + Drizzle impls in `adapters/persistence/repositories` · DrizzleUnitOfWork (tx-bound repos + EventBuffer drained strictly after commit; rollback never dispatches — proven by test) · idempotent seed (`runSeed` + `db:seed` CLI, deterministic 5eed… uuids, ON CONFLICT DO NOTHING) · 13 integration tests green against local Postgres 17 (Docker, port 54329): idempotency unique, one-active-sub, one-live-grant, entitlement predicate (strict `>` at expiry boundary), uow atomicity, seed idempotency. Migration `0000` applies cleanly via drizzle migrator on PG17.
- **UNBLOCKED (2026-07-04): dev Supabase fully credentialed.** `.env` complete: DATABASE_URL (pooler `aws-1-us-west-2.pooler.supabase.com:6543`, user `postgres.cfjrxassteeevkrodttj`) + DATABASE_DIRECT_URL both verified with live queries (PG 17.6) · SUPABASE_SERVICE_ROLE_KEY (legacy service_role JWT via `supabase projects api-keys`) verified against Storage API · private bucket `drops` created and confirmed · dev BOT_TOKEN verified via getMe (@Cabanatelebot "CABANABOT"). Note: db password + bot token passed through chat — rotate before anything real. `supabase link` ran; `supabase/.temp/` gitignored. Tyler ran `pnpm db:migrate` + `pnpm db:seed` successfully.
- **M2 COMPLETE — closeout verified (2026-07-04):** dev Supabase has all 12 tables + 11 public enums + 1 recorded migration · seed present (Demo Creator `5eed…0002`, 1 user, 1 plan, 3 drops/assets, bot+system settings; transactional tables empty as expected) · `drops` bucket private · typecheck ✓ lint ✓ 51 unit ✓ 13 integration ✓ (local PG17 Docker). M1+M2 committed by Tyler.
- **Session 5 (2026-07-04): M3 COMPLETE — core business layer.** Delivered: 6 ports (Payment shaped on real Stars lifecycle createIntent→awaitApproval→confirm→refund · Content · ContentTransport · Cache Redis-shaped incl. withLock skip-semantics · Notifier with sent/blocked/failed · Clock) · services (Access = the oracle; Audit with Zod action/entity vocabulary; User ensureRegistered; Creator requireActive guard; Drop create/addAsset/publish mirroring DB CHECKs; Purchase = the state machine TX1-pending → provider outside tx → TX2 finalize, idempotency replay returns original outcome; Subscription subscribe/renew/expireLapsed — renewal extends from max(now, expiresAt)) · engines (Delivery: access→ContentProvider→Transport, I/O outside tx, protect always true; Notification: intent queue, drain, blocked→users.is_blocked, failed→requeue) · handlers (payment-failed/sub-activated/sub-expired intents; audit enrichment `event.*` rows; analytics no-op) · adapters (MockPaymentProvider injectable random; SupabaseStorageProvider — only @supabase/* import; Memory+Noop cache) · repo extensions (payment/purchase mark*, findByPaymentId, subscription findActive/renew/listLapsed/markExpired w/ status guards, drops.publish, users.setBlocked). Tests: 125 unit (hand-written fakes in tests/fakes: MemoryStore w/ constraint enforcement + cloning proxy, FakeUow w/ snapshot-rollback, scriptable FakePaymentProvider, FakeClock) + 19 integration. **Verified end-to-end** via composition-boundary script (recipe persisted in `.claude/skills/verify/SKILL.md`): full journey incl. real Supabase Storage signed-URL fetch. eslint: added argsIgnorePattern `^_`.
- **Interpretations flagged for Tyler (Session 5):** (1) ~~ContentUnlocked emitter conflict~~ — **RULED (ADR-019, 2026-07-04): DeliveryEngine emits ContentUnlocked after actual delivery (first per user+drop via audit lookup); PurchaseService emits PurchaseCompleted only. Applied before M4.** (2) Audit vocabulary extended with `purchase.failed` and enrichment namespace `event.purchase_completed`/`event.content_unlocked` (varchar-by-design accommodates; Zod list updated). (3) publishDrop requires ≥1 asset (invented guard: empty drop delivers nothing). (4) SubscriptionActivated is raised on renewal too (no separate renewal event exists; audit distinguishes via subscription.renewed).
- **Session 6 (2026-07-04): M4 COMPLETE — Telegram adapter.** Delivered: Telegraf factory + polling/webhook launch · context and middleware chain (correlation/log/rate-limit/auth/error) · pure views/keyboards and Zod-validated callbacks · `/start`, `/help`, `/browse` pagination/detail/delivery, `/unlock`, `/subscribe`, `/my_access` · signed-URL TelegramContentTransport with `protect_content`, stale-file-id fallback, and tenant-scoped `transport_cache` merge · TelegramNotifier with sent/blocked/failed mapping · `app.ts` composition/event registration + graceful boot/shutdown in `index.ts`. ADR-019 applied: DeliveryEngine emits first-delivery `ContentUnlocked` only after every asset is sent; PurchaseService emits `PurchaseCompleted` only. Added `PORT` config for webhook listener and tenant-scoped repo support for transport cache/first-delivery lookup. Verification: typecheck ✓ lint ✓ 142 unit ✓ 21 integration (local PG17) ✓ build ✓.
- **Session 7 (2026-07-04): M5 COMPLETE — jobs & lifecycle.** Delivered: `jobs/scheduler.ts` (interval timers, per-job advisory cache lock via CacheProvider.withLock skip-semantics, crash isolation, fresh per-run correlation id → propagated into services → audit rows, run metrics hook seam [default no-op] + structured logs, graceful start/stop awaiting in-flight) · four job factories (subscription-expiration → SubscriptionService.expireLapsed · notification → NotificationEngine.drainPending · cleanup → new PurchaseService.failStalePending · analytics no-op stub, intervalMs 0 = registered-not-scheduled) · `PurchaseService.failStalePending` (pending pair older than TTL → failed, guarded/idempotent, audits as `job`, raises PaymentFailed) + repo methods `payments.listStalePending`/`markFailedIfPending` (guarded, Drizzle + memory fake) · wired in `app.ts` via `buildScheduler` — **scheduler.start() before the blocking polling `bot.launch`, scheduler.stop() (awaits in-flight) before pool close**. Tests: 161 unit (new: scheduler behaviors, four job factories incl. no-duplicate-notification end-to-end via the PaymentFailed handler, failStalePending idempotency + rollback/after-commit) + 25 integration (new: listStalePending window/order/bound, markFailedIfPending guard on real PG17). typecheck ✓ lint ✓ (boundaries: jobs/ imports no adapters) build ✓ git diff --check ✓. Not yet committed (awaiting Tyler).
- **Deferred in M5 (debt #13):** cleanup.job orphaned-storage / transport_cache pruning is a log-only stub — destructive pruning needs a ContentProvider bucket-list capability + asset cache-expiry metadata (a schema/port change, i.e. a human-reviewed migration, out of M5 scope).
- **Session 8 (2026-07-04): M6 COMPLETE — production hardening & deploy.** Delivered: `src/server/health.ts` (createHealthCheck — structural `Pingable`, DB `select 1` liveness) + `src/server/http-server.ts` (one Node http server on PORT: `GET /health` 200 ok/503 degraded/405/404 + webhook route) · `database.ping()` on the db client · bot.ts refactor (`startTelegramPolling` [dev] · `createTelegramWebhookHandler` [sync, secret-token] · `registerTelegramWebhook`/`deleteTelegramWebhook`, shared BOT_COMMANDS) — replaces `launchTelegramBot` · env: **production requires BOT_MODE=webhook** (ADR-020) · constants APP_VERSION/HEALTH_PATH/SHUTDOWN_TIMEOUT_MS · `loggingJobMetrics` monitoring hook wired into the scheduler · **app.ts**: http server + health, start order = ping → http listen → scheduler → (webhook: register | polling: launch), stop = http stop → deleteWebhook/bot.stop → scheduler drain → pool close · **index.ts**: secret-free startup diagnostics, mock-payment-in-prod warning, graceful shutdown with 10s force-timeout + uncaughtException/unhandledRejection → fatal+shutdown · `railway.json` (Nixpacks, single replica [per-process cache debt #1/#5], healthcheck `/health`) · `docs/DEPLOYMENT.md` + `docs/LAUNCH_CHECKLIST.md` (security review — no blocking findings — + manual QA) · ADR-020 · package.json 0.1.0. Tests: 172 unit (+11: http-server, health, prod-env) + 25 integration. **E2E production verification on the compiled dist**: `/health` 200 ok / 404 / 503-when-db-down; env parse-or-crash lists offending keys; prod+polling rejected; full boot logs `database reachable`→`http server listening`→jobs scheduled→clean startup-failure exit. typecheck ✓ lint ✓ build ✓ git diff --check ✓. Not committed (awaiting Tyler).
- **Owner action to go live (not code):** apply reviewed migrations to prod DB, create prod bot token + Railway secrets, set NODE_ENV=production/BOT_MODE=webhook/distinct WEBHOOK_URL path, deploy + run LAUNCH_CHECKLIST smoke, then tag `v0.1.0-mvp`.
- **M7.0–M7.1 COMPLETE (committed 2026-07-05, validated by Tyler 2026-07-06): multi-creator routing + creator dashboard API.** Post-MVP adoption phase begun per the M7 plan trio. M7.0: `creators.slug` (migration `0001`) + `follows` table (migration `0004`); `CreatorResolver`/creator-context at the Telegram edge; `CreatorService.findBySlug`/`listActive`; `FollowRepository`; `/start` deep-link payload now parsed. The single-creator `SEED_IDS` pin in app.ts is removed (repays debt #11). M7.1: new REST/JSON adapter `src/adapters/api/` (http.ts + router.ts) mounted on the M6 `HttpServer` — routes for auth (register/login/logout), `/api/me`, profile, content drops (+assets/publish), plans, analytics summary, onboarding, and public marketplace (creators/featured/categories/profile); `AuthService` + new ports `PasswordHasher` (scrypt) and `SessionTokenService` (opaque 256-bit bearer, SHA-256-at-rest) in `src/adapters/auth/`; `creator_identities` table (migration `0002`); `creators.user_id` now nullable (web-only creators). **Auth is hand-rolled behind ports, NOT the managed provider (Supabase Auth/Clerk) the M7 plan recommended — see Open items.** Validated by Tyler 2026-07-06.
- **M7.2 COMPLETE (committed 2026-07-05, validated by Tyler 2026-07-06): creator onboarding flow.** `onboarding.service.ts` + `/api/onboarding` and `/api/onboarding/complete` routes; `creators.onboarding_completed_at` (migration `0003`); `pending → active` transition on completion. Reuses M7.1 identity/API; no new auth surface.
- **M7.3 COMPLETE (committed 2026-07-05, validated by Tyler 2026-07-06): marketplace & discovery.** `discovery.service.ts` + `follow.service.ts`; public marketplace/search API routes (`/api/public/creators`, `featured`, `categories`, creator profile); bot-side `/follow`. **Correction (M7.3.1 audit): bot-side `/discover`/`/search` were never built (marketplace discovery is API-only), and the pg_trgm/GIN search indexes were not shipped at M7.3 — both were reconciled/delivered in M7.3.1.**
- **M7.3.1 COMPLETE (platform hardening — this session, awaiting Tyler review/commit): closed every HIGH/MEDIUM from the whole-repo architecture audit before M7.4.** Additive + boundary-preserving; no redesign. Delivered: (1) **API rate limiting** — `src/adapters/api/rate-limit.ts`, CacheProvider-backed per-IP (+per-email) on login/register before scrypt runs and per-creator over all authenticated routes incl. uploads; `rate_limited`→429 + `Retry-After`; new env `API_AUTH_RATE_LIMIT_*` / `API_RATE_LIMIT_*`. (2) **Subscription correctness** — migration `0005` replaces the per-plan partial unique index with **one-active-per-creator** `(user_id, creator_id) WHERE status='active'`; `findActiveForUserAndCreator` now `ORDER BY expires_at DESC`; finalize re-asserts planId and maps the unique violation to a graceful `conflict`; new integration regression `subscriptions-concurrency.test.ts` proves concurrent distinct-plan subscribes yield exactly one charge. (3) **Notification-queue port** — `core/ports/notification-queue.port.ts` + `adapters/notifications/in-memory-notification-queue.ts`; `NotificationEngine` delegates storage to the port (M7.4 drops in a shared impl with no core change); distributed drain still M7.4 (debt #14). (4) **Marketplace/sweep indexes** — migration `0006`: pg_trgm GIN on display_name/slug, partial btree for the discoverable() predicate/sort, partial index for the stale-pending payment sweep. (5) **`/my_access` N+1 removed** — `AccessService.resolveAccessForDrops` batches the catalog (one sub-check per creator + one grants query) + repo `findLiveGrantsForDrops`. (6) **ESLint boundary closure** — every `adapters/*` subdir is an isolation target, `server/` zoned, `@supabase`/`telegraf`/`drizzle-orm` confined to their owning adapter, guard test `tests/unit/config/eslint-boundaries.test.ts`. (7) **Version/doc reconciliation** — APP_VERSION/package.json → `0.2.1`. **Post-review hardening (from the adversarial diff review): the API rate-limiter now resolves client IP from the rightmost trusted-proxy X-Forwarded-For hop (validated as an IP; `API_TRUSTED_PROXY_HOPS=1`) so a spoofed leftmost XFF can't bypass the throttle or collide with the per-email bucket (buckets namespaced `ip:`/`email:`); public marketplace reads gained a per-IP throttle (`API_PUBLIC_RATE_LIMIT_*`); added the TX2-conflict rollback unit test, denied-outcome batch-equivalence test, and a behavioral ESLint-confinement test.** Migration `0005` carries a documented pre-apply dedup check (owner action). Tests: 236 unit + 40 integration, typecheck ✓ lint ✓ build ✓.
- **M7.4 COMPLETE (Redis + horizontal scaling — this session, awaiting review/commit; deploy owner-gated).** Implements the reserved Redis path so rate limits, job locks, creator-context sessions, and the notification queue become replica-shared (repays debt #1 + #14; unpins `numReplicas>1`). Additive, behind the existing `CacheProvider`/`NotificationQueue` ports; NO DB changes. Added dep: **ioredis 5.11**. New files: `src/adapters/cache/redis-client.ts` (shared connection factory, idempotent close, `commandTimeout` 2s), `redis-cache.provider.ts` (`RedisCacheProvider`: atomic `incr`+TTL-on-create via Lua, distributed `withLock` = SET NX PX + fencing token + compare-and-del release, skip-not-wait), `src/adapters/notifications/redis-notification-queue.ts` (`RedisNotificationQueue`: Redis list, atomic Lua drain, FIFO), + real-Redis integration tests. `app.ts`: `buildCacheInfra(env,clock)` builds ONE shared Redis connection for both providers when `CACHE_PROVIDER=redis`, closed on shutdown/startup-failure; NotificationEngine gets the shared queue. `env.ts`: `REDIS_URL` required when `CACHE_PROVIDER=redis`. `eslint.config.js`: ioredis confined to cache+notifications adapters (behavioral guard test). **Post-review hardening (adversarial diff review): bounded `commandTimeout` + FAIL-OPEN rate limiting on BOTH channels** (a Redis outage degrades to no-throttle, never a total outage — `checkRateLimit` + `rateLimitMiddleware` catch cache errors and allow), idempotent connection close (fixes startup-failure double-close), bounded notification drain (500/tick, matches the M5 sweeps). Redis-backed live settings (debt #4) deferred — needs a settings consumer. Tests: **244 unit + 52 integration** (incl. 12 real-Redis); typecheck ✓ lint ✓ build ✓. **DEPLOY GATE:** `railway.json` stays `numReplicas:1`; raise it ONLY after provisioning Railway Redis + setting `CACHE_PROVIDER=redis`+`REDIS_URL` (else 2 replicas on memory cache reintroduces the bugs). Production is still on M6 (see prod-state note above) — M7.0–M7.4 remain undeployed.
- **Migration high-water mark: `0006`.** Since MVP: `0001` slug, `0002` creator_identities, `0003` onboarding_completed_at, `0004` follows, `0005` one-active-per-creator subscription index (**constraint replacement** — drops `subscriptions_one_active_per_plan_uq`, creates `subscriptions_one_active_per_creator_uq`), `0006` marketplace/sweep indexes + `pg_trgm` extension. `0005`/`0006` verified against local PG17 (Docker :54329) by the integration suite and the real `db:migrate` command.
- **Production deployment state (CONFIRMED 2026-07-06 via Railway CLI + read-only probes):** Railway project `telegramtech-dev`, env `production`, service `chic-unity` (Online) at `https://chic-unity-production-f527.up.railway.app`. **Deployed code = commit `d22cbf1` = M6 (`v0.1.0-mvp`)** — `/health` reports `version:"0.1.0-mvp"`, DB `ok`; all `/api/*` routes return the server-level 404 (no API mounted). **So M7.0–M7.3 (and M7.3.1) are committed to `main` but NOT deployed** — Railway did not auto-deploy the M7 commits. Bot is live in **webhook mode**, secured (`POST /telegram/webhook` without the secret → 403). **Prod and dev share ONE Supabase project** (`cfjrxassteeevkrodttj`, `aws-1-us-west-2` pooler). **Shared DB schema is at migration `0004`** (M7.0–M7.3 columns/tables present: slug, onboarding_completed_at, category, is_featured, creator_identities, sessions, follows); `0005`/`0006` NOT yet applied (old `subscriptions_one_active_per_plan_uq` still present, no `pg_trgm`). **DB is thus schema-ahead of the M6 code but behind M7.3.1.** M7.3.1 API/scale env vars are NOT set in prod. 0005 pre-apply dedup check run against the shared DB: 1 active subscription, **0 blocking duplicates → 0005 is safe to apply.** Deploying M7.3.1 to prod is a manual M6→M7.3.1 upgrade (commit+push, apply 0005+0006, set env vars, redeploy, smoke) — an owner-gated action, not done in this validation pass.
- **ADRs shipped-in-code but not written:** M7.0–M7.3.1 introduced hybrid-bot routing, a web identity distinct from Telegram users, (still-pending) RLS, and the notification-queue port — proposed ADR-021 (one-active-per-creator) / ADR-022 (NotificationQueue port) / ADR-023 / ADR-025 are referenced but ARCHITECTURE_DECISIONS.md still stops at ADR-020. See Open items.
- **MVP (M0–M6) is code-complete and released (tag `v0.1.0-mvp`, 2026-07-04). Post-MVP work: the M7 adoption phase (M7.0–M7.3) is committed and validated (2026-07-06); M7.3.1 platform hardening is complete (awaiting review/commit).** The M7 plan trio (evolution plan, implementation plan, current-state audit in docs/) is the source of truth for M7 scope and order. Next undelivered milestone is **M7.4 (Redis + horizontal scaling)** — do not begin it, or any later M7 milestone (real Stars/payouts, admin, dedicated bots, public API), without Tyler's per-milestone go-ahead. **M7.4 must include externalizing the notification-queue drain (debt #14) before `numReplicas>1`.**

## Decisions locked this session (do not re-litigate)
- Q1: Supabase Storage is content source of truth (private bucket, signed URLs); telegram file_id only as rebuildable `transport_cache`.
- Q2: drops.access_type = free | premium | pay_per_unlock (CHECK ties price to unlock type).
- Q3: one Premium plan seeded; schema tier-ready.
- Q4: MOCK_PAYMENT_FAILURE_RATE stays; failure UX built + tested in MVP.
- Q5: GitHub Actions CI from M1 (typecheck, lint incl. boundary rules, unit tests).
- ADR-011: premium entitlement = LIVE subscriptions check; access_grants = pay-per-unlock + manual only; no subscription grants, no grant revocation sweep.
- ADR-010: in-process domain events (PurchaseCompleted, PaymentFailed, SubscriptionActivated, SubscriptionExpired, ContentUnlocked), synchronous, AFTER COMMIT, isolated handlers, registered in app.ts. No broker. Outbox = future seam.
- ADR-006/007: ContentProvider + CacheProvider ports; MVP impls SupabaseStorage + MemoryCache(+Noop).
- ADR-012: creator_id tenancy on every tenant-owned table; repositories always tenant-filter.
- Jobs layer: scheduler + subscription-expiration, notification, cleanup, analytics(no-op stub).
- Provider enum value renamed `mock` (provider-neutral). New table: drop_assets.
- **M7 adoption-first reprioritization (locked 2026-07-06):** post-MVP work follows the M7 plan trio's adoption-first order — M7.0 multi-creator routing → M7.1 creator dashboard (API) → M7.2 onboarding → M7.3 marketplace/discovery **before** M7.4 (Redis/scale), M7.5 (real Stars + payouts), M7.6 (admin/ops), M7.7 (dedicated bots), M7.8 (public API/channels). Getting creators onboarded and selling comes ahead of scale/monetization-hardening. Do not re-litigate the ordering; individual milestones still require Tyler's per-milestone go-ahead before build.

## Non-negotiables (carried + revised)
1. core/ imports only shared/. No Telegraf/Drizzle/@supabase anywhere in core. ESLint-enforced in CI.
2. Clients → services → repositories → DB; adapters never import each other.
3. PaymentProvider port shaped on REAL Stars lifecycle; mock adapts to it.
4. Purchases (commerce) ≠ access_grants (entitlement ledger). Audit rows in-transaction; events after commit.
5. Result<T, AppError> for expected failures. Env access only in config/env.ts.
6. Migrations only from approved schema, human-reviewed.

## File map (docs/)
SYSTEM_ARCHITECTURE.md (authoritative; supersedes ARCHITECTURE.md) · DATABASE.md rev 2.2 (12 physical tables) · ARCHITECTURE_DECISIONS.md ADR-001..020 (supersedes DECISIONS.md) · SETUP.md rev 2 · ROADMAP.md rev 2 (M0–M6, 12 debts) · README.md · this file · M7 plan trio: M7_PLATFORM_EVOLUTION_PLAN.md · M7_IMPLEMENTATION_PLAN.md · M7_CURRENT_STATE_AUDIT.md (governing M7.0–M7.8; adoption-first order).

## Session 3 plan (= M1, on approval) — DONE ✅
1. ~~Scaffold: pnpm, strict tsconfig, ESLint boundary rules (import/no-restricted-paths mirroring the dependency graph), Prettier, Vitest.~~
2. ~~CI workflow (typecheck/lint/unit).~~
3. ~~config/env.ts (Zod, parse-or-crash) + tests · logging/logger.ts (Pino, redaction) · shared/ (Result, AppError, domain types).~~
4. ~~core/events dispatcher + tests (after-commit ordering, handler isolation).~~
5. STOP. Review before M2 (schema → migration → review → repositories). ← **we are here**

## Session 4 plan (= M2, on M1 approval)
1. Drizzle + drizzle-kit deps · `drizzle.config.ts` · schema exactly per DATABASE.md rev 2.
2. Generate migration → **STOP for human review** → apply via DATABASE_DIRECT_URL.
3. Unit-of-work/tx helper (drains EventBuffer into dispatcher after commit) · repositories + integration tests.
4. Idempotent seed (creator, Premium plan, drops of all three access types, settings).
Note: the "As S1" column gaps are resolved — DATABASE.md rev 2.2 (2026-07-04) fully specifies audit_logs and system_settings inline (rev 2.1 approved by Tyler with revisions: varchar not enum for action/entity_type, system_settings category + updated_by, AuditRepository append-only contract). Rev 2.2 awaits Tyler's go-ahead before schema/migration generation.

## Open items for Tyler
- ~~Approve DATABASE.md rev 2~~ — **approved 2026-07-04**; review M1 output next.
- Confirm working name: repo "creator-platform"? (docs use it; cosmetic — package.json uses creator-platform, dir is telegramtech).
- Supabase: confirm a dev project + private `drops` bucket exist before M2.
- ~~Spec the exact audit_logs + system_settings columns~~ — done; rev 2.1 approved with revisions, applied as rev 2.2 (2026-07-04). **Confirm rev 2.2 → M2 (Drizzle schema + migration generation) may begin.**
- ~~**Validate M7.0–M7.3.**~~ — **validated by Tyler 2026-07-06**; all four marked ✅ complete above.
- **RLS debt (#2) trigger is now TRIPPED.** The debt register's own trigger for RLS is "first non-bot client (dashboard/API)" — that client (`src/adapters/api/`) is now live, but no table has RLS enabled (`isRLSEnabled: false` throughout the migration snapshots). Today tenant safety rests solely on the API routing through core services (which tenant-filter). Decide when to apply the `creator_id` RLS activation (proposed ADR-025); the M7 implementation plan wanted it right after the M7.1 read path, the evolution plan at M7.3 — both points have passed.
- **Missing ADRs.** ADR-021 (hybrid bot model / CreatorResolver), ADR-023 (creator web identity distinct from Telegram users), and ADR-025 (RLS activation) were implemented but never written; ARCHITECTURE_DECISIONS.md stops at ADR-020. Project rule requires a new ADR per significant decision — write these to close the gap.
- **No web frontend in this repo.** M7.1/M7.2 delivered the dashboard/onboarding *API + backend only*; there is no `dashboard/`/`web/`/`apps/` directory. The Next.js dashboard the M7.1 plan calls for is absent, so the adoption-phase user-facing definition of done is not met — confirm whether the frontend lives elsewhere or is still to be built.
- **Hand-rolled auth deviation.** M7.1 auth uses a scrypt `PasswordHasher` + `node:crypto` opaque bearer tokens (behind ports) instead of the managed provider (Supabase Auth / Clerk) the M7 plan recommended. Clean and testable, but confirm this is an accepted deviation rather than an oversight.
- **Confirm M7 migrations applied.** Verify `0001`–`0004` are applied to dev (and, when going live, prod) Supabase; the docs don't record this.
